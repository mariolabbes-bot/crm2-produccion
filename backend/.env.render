# ================================================
# VARIABLES DE ENTORNO PARA RENDER
# ================================================
# Copia estas variables en Render.com:
# Dashboard → Tu servicio → Environment
# ================================================

# Entorno
NODE_ENV=production
PORT=10000

# Base de Datos Neon PostgreSQL (PRODUCCIÓN)
DATABASE_URL=postgresql://neondb_owner:npg_DYTSqK9GI8Ei@ep-rapid-sky-ace1kx9r-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require

# CORS - Orígenes permitidos (separar con comas si hay varios)
CORS_ORIGINS=https://crm2-produccion.vercel.app,http://localhost:3000

# JWT Secret para autenticación
# IMPORTANTE: Genera uno nuevo con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=GENERAR_UN_SECRET_ALEATORIO_AQUI_CAMBIAR_EN_PRODUCCION

# Google Maps API (opcional)
GOOGLE_MAPS_API_KEY=tu_api_key_aqui_si_la_tienes

# Logging
LOG_LEVEL=info
