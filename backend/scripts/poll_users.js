const https = require('https');

// Create a small ad-hoc server endpoint test using our existing poll format to query the users without deploying new code.
// Wait, I can't just run SQL from outside. I have to deploy the endpoint!
